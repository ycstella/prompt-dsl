## promtDsl pCodeGen

### 调用层级
#### pCodeGen.go
###### PCodeGen 
PCodeGen.Pcodegen()
 ├─ validateArgs()
 ├─ initGenDirs()
 ├─ parserAndGen()
 │    └─ NewPromptToGenCode()
 │    └─ PromptToGenCode()
 ├─ copyGoFiles()
 ├─ buildExe()
#
#### runner.go
###### promptToGenCode
 PromptToGenCode()
 ├─ parsePrompt()
 ├─ astToNode()
 ├─ buildPGCxtAndToCode()
 ├─ genCode()
 │    ├─ NewCodeBuilder()
 │    ├─ CodeBuilder.combineSingleCode()
 │    ├─ installGoImports()
 │    └─ os.WriteFile()
 ├─ ensureGoModule()
 │    ├─ os.Stat()
 │    ├─ exec.Command("go mod init ...")
 ├─ runGoGet()
 │    ├─ exec.Command("go get ...")
 ├─ runGoModTidy()
 │    ├─ exec.Command("go mod tidy")

#### 1. 下面功能独立出来 promptToGenCode --->PCodeGen
├─ ensureGoModule()
 │    ├─ os.Stat()
 │    ├─ exec.Command("go mod init ...")
 ├─ runGoGet()
 │    ├─ exec.Command("go get ...")
 ├─ runGoModTidy()
 │    ├─ exec.Command("go mod tidy")
### 2.添加命令行入口参数验证功能
![alt text](image-2.png)
```

NAME:
   pCodegen - 提示词生成解析执行工具

USAGE:
   pCodegen [global options] command [command options]

COMMANDS:
   help, h  Shows a list of commands or help for one command

GLOBAL OPTIONS:
   --outputName value, -o value  生成的 exe 文件名（必选）
   --promtPath value, -p value   .pdsl 文件所在文件夹路径（必选）
   --actionType,  -createPDSL: 创建新的PDSL文件|- generateSource： 验证并生成源代码|build&Run| runExistingExe: 在AI上运行|
   --help, -h                    show help
```
![alt text](image-1.png)
	第三方轻量级库 github.com/urfave/cli/v2     实现入口参数验证	
### 3. 新生成的代码部分
## Interface definition
![alt text](image.png)
+ 每个task都实现该接口中的functions。
+ CodeBuilder 生成具体的task的结构和方法实现。并存放于xxxTaskPackage下面。例如splitStepsTask, 会创建对应的包和具体的代码实现



## main.go 伪代码实例
``` go
func AiOperation(task Task, jsonFile, YamlFile) {
   	task.validateInput();
	task.init(jsonFile, YamlFile);	
	task.run();   	
}

func main() {
    //Input Parameters validation such as check pamameter is missing or not.
	validateParemeter(); 
    //following statement is generated by tool
    task = &SplitStepsTask;
	//generation end
	AiOperation(task, jsonFile, YamlFile, modelName);
}
```
``` go
	if strings.TrimSpace(c.promptNode.FixCode[0]) == "" && strings.TrimSpace(c.promptNode.AfterCode[0]) == "" {
		if err := c.buildExecuteDir(); err != nil {
			return err
		}
	} else {
		if err := c.buildExecutePipeline(); err != nil {
			return err
		}
	}
```
***
### 4. 现有代码逻辑
//下面两个函数体区别是？
GenSys() 入参是否需要？
``` go
    if c.loopClass != "" {
		c.b.WriteString(fmt.Sprintf("func (p *" + c.fileName + ")GenSys(in " + c.fileName + "InputContext,it " + c.loopClass + ") string {\n"))
	} else {
		c.b.WriteString(fmt.Sprintf("func (p *" + c.fileName + ")GenSys(in " + c.fileName + "InputContext) string {\n"))
	}

```
***
``` go
	if c.loopClass != "" {
		c.b.WriteString("    sys := p.GenSys(p.currentData,p.it)\n")
		c.b.WriteString("    user := p.GenUser(p.currentData,p.it)\n")
	} else {
		c.b.WriteString("    sys := p.GenSys(p.currentData)\n")
		c.b.WriteString("    user := p.GenUser(p.currentData)\n")
	}

//新的方法？
func (p *SplitSolutionSteps)GenSys() string {
    var b strings.Builder
    b.WriteString("你是一个擅长拆分解题步骤的数学老师 \n")
    b.WriteString("当前任务是将解题步骤进行合理拆分\n")
    return b.String()

}
	
```
### 5.下面代码重复的逻辑
//func run()
buildExecuteDir()
buildExecutePipeline()

- buildExecutePipeline() 和 buildExecuteDir()逻辑基本重复，是否可以优化？
- GenSys 函数可以优化吗？
- p.GenSys(p.currentData,p.it) 
- GenSys(p.currentData) 

buildSingleMain
buildExeSingleMain
buildSingleLExecute
buildLoopMain
``` go
	if strings.TrimSpace(c.promptNode.FixCode[0]) == "" && strings.TrimSpace(c.promptNode.AfterCode[0]) == "" {
		if err := c.buildExecuteDir(); err != nil {
			return err
		}
	} else {
		if err := c.buildExecutePipeline(); err != nil {
			return err
		}
	}
```

- 代码规范建议，Tab键缩进统一用4个空格替换。保证在不同平台下代码的一致性。IDE 里面设置indent，用lint工具增强代码规范。
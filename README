# PromptDSL 使用流程说明

---

## 1. 下载 pCodeGen

使用 `go install` 下载指定版本的生成工具：

```bash
go install github.com/along416/promptDSL/cmd/pCodeGen@v0.1.7
```

> 下载完成后，生成的可执行文件一般位于 `$GOPATH/bin` 目录下，例如：
>
> `C:\Users\Administrator\go\bin\pCodeGen.exe`

---

## 2. 撰写 PromptDSL 文件

在指定目录下撰写 `.pdsl` 文件，例如：

```text
D:\work\test\prompts\example.pdsl
```

---

## 3. 运行 pCodeGen 生成 exe 文件

使用命令生成可执行文件：

```powershell
& "C:\Users\Administrator\go\bin\pCodegen.exe" "../../run/Runner" "D:\work\test\prompts\"
```

### 参数说明：

- **pCodeGen 下载路径**  
  `C:\Users\Administrator\go\bin\pCodegen.exe`  

- **生成的 exe 文件名**  
  `"../../run/Runner" + xxx`（`xxx.pdsl` 对应生成的程序）  

- **.pdsl 文件所在文件夹**  
  `"D:\work\test\prompts\"`  

> 执行后会在 `../../run/` 目录生成对应的 `.exe` 文件，例如：
>
> ```text
> ../../run/RunnerExample.exe
> ```

---

## 4. 运行生成的 exe

运行生成的程序，并准备好配置文件和日志文件：

```powershell
.\run\RunnerCheckSolutionSteps.exe "input.json" "config"
```

### 参数说明：

- **程序地址**  
  `.\run\RunnerCheckSolutionSteps.exe`  

- **输入文件路径**  
  `"input.json"`  

- **配置文件所在文件夹**  
  `"config"`  

> 确保 `config` 文件夹中包含程序运行所需的配置和日志文件。



